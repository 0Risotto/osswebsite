[build]
  command = "pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22.22.2"
  NPM_FLAGS = "--version"
  
# Enable pnpm specifically
[build.environment]
  NETLIFY_USE_PNPM = "true"

[[plugins]]
  package = "@netlify/plugin-nextjs"